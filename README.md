# Travel-Buddy-Finder
Travel Buddy Finder is a Django backend project that manages travel trips. It allows users to create and view trips using a SQLite database and JSON APIs. The project demonstrates Django models, views, URLs, and basic backend functionality for academic use.
